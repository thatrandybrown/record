(document.onLoad = () => {
    const textarea = document.querySelector('textarea');
    const nav = document.querySelector('nav');

    let record = window.localStorage.getItem('record');
    let metaData = window.localStorage.getItem('record_app_metadata');

})();
